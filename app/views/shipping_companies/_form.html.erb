<%= form_with(model: shipping_company, local: true) do |form| %>
  <% if shipping_company.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(shipping_company.errors.count, "error") %> prohibited this shipping_company from being saved:</h2>

      <ul>
      <% shipping_company.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <table class=" table left no-border ">
    <tbody>
      <tr class="field">
        <td><strong>Name: </strong></td>
        <td><%= form.text_field :name %></td>
      </tr>
    </tbody>
  </table>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
